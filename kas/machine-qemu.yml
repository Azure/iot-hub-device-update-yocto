header:
  version: 14
  includes:
    - base.yml

machine: qemuarm64
target: adu-base-image

repos:
  meta-qemu-adu:
    path: ../meta-qemu-adu

local_conf_header:
  qemu: |
    # QEMU specific
    IMAGE_BOOT_FILES = "Image boot.scr"
    UBOOT_MACHINE = "qemu_arm64_defconfig"
