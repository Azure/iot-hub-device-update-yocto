header:
  version: 14

# This file is kept for backward compatibility but is no longer needed.
# base.yml and machine configs are local-first (no URLs by default).
# For CI/remote builds, use remote.yml to add git URLs.
#
# Usage (local dev):
#   kas build kas/base.yml:kas/machine-qemu.yml
#
# Usage (CI with auto-clone):
#   kas build kas/base.yml:kas/machine-qemu.yml:kas/remote.yml
